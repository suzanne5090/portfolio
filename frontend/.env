# Disable the ESLint webpack plugin — it crashes with ajv@8 (defaultMeta error).
# ESLint linting still runs locally; this only disables the webpack build-time check.
DISABLE_ESLINT_PLUGIN=true

# Skip CRA preflight dependency checks
SKIP_PREFLIGHT_CHECK=true
